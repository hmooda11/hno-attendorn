{
  "name": "hno-praxis-attendorn",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/build-sites.mjs",
    "build:pages": "vite build --outDir docs --emptyOutDir",
    "build:vite": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^7.0.5"
  }
}
